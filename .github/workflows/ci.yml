name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    name: test (${{ matrix.distro }})
    runs-on: ubuntu-22.04
    container:
      image: ${{ matrix.image }}
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
          - distro: debian
            image: kcov/kcov:latest
            coverage: 'true'
            primary: 'true'
          - distro: arch
            image: archlinux:latest
            coverage: 'true'
            primary: 'false'
          - distro: fedora
            image: fedora:latest
            coverage: 'true'
            primary: 'false'
          - distro: alpine
            image: alpine:latest
            coverage: 'true'
            primary: 'false'

    steps:
      - name: Install dependencies (debian)
        if: matrix.distro == 'debian'
        run: |
          apt-get update && apt-get install -y --no-install-recommends \
            ca-certificates make git bats curl

      - name: Install dependencies (arch)
        if: matrix.distro == 'arch'
        run: pacman -Sy --noconfirm --needed git make diffutils bats curl kcov

      - name: Install dependencies (fedora)
        if: matrix.distro == 'fedora'
        run: dnf install -y git make diffutils bats curl kcov

      - name: Install dependencies (alpine)
        if: matrix.distro == 'alpine'
        shell: sh
        run: |
          apk add --update --no-cache \
            bash git make bats coreutils curl diffutils \
            cmake g++ python3 \
            curl-dev curl-static libdw openssl-dev zlib-dev elfutils-dev libstdc++-dev
          git clone --depth 1 https://github.com/SimonKagstrom/kcov.git /tmp/kcov
          cmake -S /tmp/kcov -B /tmp/kcov/build -DCMAKE_BUILD_TYPE=Release
          cmake --build /tmp/kcov/build -j$(nproc)
          cmake --install /tmp/kcov/build

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Run tests with coverage
        if: matrix.coverage == 'true'
        run: make test

      - name: Upload per-distro coverage
        if: matrix.coverage == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.distro }}
          path: test-results/coverage/

      - name: Upload test report XML
        if: matrix.primary == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: test-report-xml
          path: test-results/*.xml

      - name: Upload release artifact
        if: matrix.primary == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: just-bashit
          path: just-bashit.tar.gz

  test-macos:
    name: test (macos)
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Install dependencies
        run: |
          brew install bash coreutils diffutils bats-core kcov

      - name: Export GNU utilities to PATH
        run: |
          echo "$(brew --prefix coreutils)/libexec/gnubin" >> "$GITHUB_PATH"
          echo "$(brew --prefix bash)/bin" >> "$GITHUB_PATH"

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Run tests with coverage
        run: make test

      - name: Upload macOS coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-macos
          path: test-results/coverage/

  merge-coverage:
    needs: [test]
    runs-on: ubuntu-22.04
    container:
      image: kcov/kcov:latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Install git
        run: |
          apt-get update && apt-get install -y --no-install-recommends \
            ca-certificates git

      - uses: actions/checkout@v4

      - name: Download coverage artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-*
          path: all-coverage

      - name: Merge coverage
        run: |
          dirs=()
          for d in all-coverage/coverage-debian all-coverage/coverage-arch \
                   all-coverage/coverage-fedora all-coverage/coverage-macos; do
            [ -d "$d" ] && dirs+=("$d")
          done
          kcov --merge merged-coverage "${dirs[@]}"

      - name: Generate coverage badge JSON
        run: |
          XML=$(find merged-coverage -name cobertura.xml | head -1)
          RATE=$(grep -oP 'line-rate="\K[^"]+' "${XML}" | head -1)
          PCT=$(awk "BEGIN { printf \"%d\", ${RATE} * 100 }")
          if   [ "${PCT}" -ge 90 ]; then COLOR="brightgreen"
          elif [ "${PCT}" -ge 75 ]; then COLOR="green"
          elif [ "${PCT}" -ge 60 ]; then COLOR="yellowgreen"
          elif [ "${PCT}" -ge 40 ]; then COLOR="yellow"
          else COLOR="red"
          fi
          printf '{"schemaVersion":1,"label":"coverage","message":"%d%%","color":"%s"}\n' \
            "${PCT}" "${COLOR}" > merged-coverage/coverage-badge.json

      - name: Upload merged coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: merged-coverage/

  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - uses: actions/checkout@v4

      - name: shellcheck (SARIF → Code Scanning)
        run: |
          shellcheck --format=sarif src/*.sh > shellcheck.sarif || true
          python3 -c "import json; json.load(open('shellcheck.sarif'))" \
            2>/dev/null || printf '%s\n' \
            '{"version":"2.1.0","$schema":"https://json.schemastore.org/sarif-2.1.0.json","runs":[{"tool":{"driver":{"name":"shellcheck","rules":[]}},"results":[]}]}' \
            > shellcheck.sarif

      - uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: shellcheck.sarif

  deploy-docs:
    needs: [merge-coverage, lint]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install zensical junit2html junitparser

      - run: zensical build --clean

      - name: Download test report XML
        uses: actions/download-artifact@v4
        with:
          name: test-report-xml
          path: test-report-xml

      - name: Generate test report HTML
        run: |
          python3 - <<'PYEOF'
          import glob
          from junitparser import JUnitXml
          xml = JUnitXml()
          for f in sorted(glob.glob('test-report-xml/*.xml')):
              xml += JUnitXml.fromfile(f)
          xml.write('test-report-xml/merged.xml')
          PYEOF
          mkdir -p site/test-report
          junit2html test-report-xml/merged.xml site/test-report/index.html

      - name: Download merged coverage report
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: site/coverage

      - name: Move badge JSON to site root
        run: mv site/coverage/coverage-badge.json site/coverage-badge.json || true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - uses: actions/deploy-pages@v4
        id: deployment

  release:
    needs: [test, test-macos, lint]
    runs-on: ubuntu-22.04
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: just-bashit

      - name: Read version
        id: ver
        run: |
          VER=$(grep 'PACKAGE: just-bashit version' src/datetime.sh \
                | grep -oP '\d+\.\d+\.\d+')
          echo "version=${VER}" >> "$GITHUB_OUTPUT"

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VER="v${{ steps.ver.outputs.version }}"
          SHORT="${{ github.sha }}"
          SHORT="${SHORT:0:7}"
          if gh release view "${VER}" >/dev/null 2>&1; then
            TAG="${VER}-${SHORT}"
          else
            TAG="${VER}"
          fi
          gh release create "${TAG}" just-bashit.tar.gz \
            --title "just-bashit ${TAG}" \
            --notes "Automated release from commit ${{ github.sha }}."
